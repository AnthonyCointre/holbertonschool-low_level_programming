#include <stdio.h>

/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 *
 * Return: Always 0.
 */

int main(void)
{
    int x;

    for (x = 0; x < 0; num++)
    {
		putchar("%d", num);
    }
    putchar('\n');
    return (0);
}
